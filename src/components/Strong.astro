---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'strong'>

const { class: className, ...props } = Astro.props
---

<strong class:list={['font-medium', className]} {...props}><slot /></strong>
